# IntraCore: параметры HAL блока из базы STM32CubeMX (db/mcu/IP/*_Modes.xml).
# Порождено tools/hal_import — руками не править. Формат: src/Docs/Description/HalCatalogFormat.md
[Ip]
Name=SBS
Version=sbs_v1_0_Cube

[Parameters]
# имя|подпись|группа|единица|мин|макс|описание
XSPIM1|IO HSLV FOR XSPIM1|Basic parameters||||If the HSLV is enabled in SBS that user must enable also the HSLV in option bytes to make this functional.
XSPIM2|IO HSLV FOR XSPIM2|Basic parameters||||If the HSLV is enabled in SBS that user must enable also the HSLV in option bytes to make this functional.
IO_OTHER|IO HSLV FOR OTHER IO|Basic parameters||||If the HSLV is enabled in SBS that user must enable also the HSLV in option bytes to make this functional.

[Values]
# параметр|значение|подпись
